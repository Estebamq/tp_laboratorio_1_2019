#include <stdio.h>

/** \brief La funcion recibe dos enteros los suma y devuelve un entero
 *
 * \param a int
 * \param b int
 * \return int un entero
 *
 */
int sumar(int a,int b);

/** \brief La funcion recibe dos enteros los resta y devuelve un entero
 *
 * \param a int
 * \param b int
 * \return int
 *
 */
int restar(int a, int b);

/** \brief La funcion recibe dos enteros los divide y devuelve un flotante
 *
 * \param a int
 * \param b int
 * \return float
 *
 */
float dividir(int a, int b);

/** \brief La funcion recibe dos enteros los multiplica y devuelve un entero
 *
 * \param a int
 * \param b int
 * \return int
 *
 */
int multiplicar(int a, int b);

/** \brief La funcion recibe un enteros realiza el factorial del mismo y devuelve un entero
 *
 * \param a int
 * \return int
 *
 */
int factorial(int a);
